name: Deploy to WordPress.org
on:
  push:
    tags:
    - "*"
jobs:
  plugin_meta:
    uses: ./.github/workflows/reusable-get-plugin-meta.yml

  tag:
    name: New tag
    runs-on: ubuntu-latest
    needs: plugin_meta
    steps:
    - uses: actions/checkout@master

    # Build plugin for production use
    - name: Plugin production build steps (Composer dual install)
      run: bash .github/scripts/plugin-production-build-steps.sh

    # Install SVN ( Subversion )
    - name: Install SVN ( Subversion )
      run: |
        sudo apt-get update
        sudo apt-get install subversion

    # Deploy to WordPress.org
    - name: WordPress Plugin Deploy
      uses: 10up/action-wordpress-plugin-deploy@master
      env:
        SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
        SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
        SLUG: ${{ needs.plugin_meta.outputs.plugin_slug }}
